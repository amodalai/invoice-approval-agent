{
  "description": "Load the demo dataset into the stores: five purchase orders, the five live invoices, and a decided backlog of ten invoices with their reviews and events. Idempotent per row: rows that are already present are skipped, so it is safe to run again. Runs from the `seed` chat command and from the UI on first open, when the invoices store is empty.",
  "parameters": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  },
  "runningLabel": "Loading the demo invoices",
  "completedLabel": "Loaded the demo invoices",
  "timeout": 60000,
  "execution": "durable",
  "uses": {
    "tools": [
      "store__purchase_orders__query",
      "store__purchase_orders__set",
      "store__invoices__query",
      "store__invoices__set",
      "store__reviews__query",
      "store__reviews__set",
      "store__events__query",
      "store__events__set"
    ]
  },
  "triggers": [
    { "kind": "invoke" },
    {
      "kind": "regex",
      "pattern": "^\\s*seed(?:\\s+examples)?\\s*$",
      "flags": "i",
      "continueToLLM": true
    }
  ]
}
